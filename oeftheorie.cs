using System;

namespace LaboOefeningen
{
    public class oefeningenTheorie
    {
        public static double Pythagoras(double a, double b) 
        {
            double som = a * a + b * b;
            return Math.Sqrt(som);
        }
    }
}